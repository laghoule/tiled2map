; GENERATED BY tiled2map (https://github.com/laghoule/tiled2map)
; DO NOT EDIT

; <scene_name> SCENE <
;   BG buffer offset,
;   FG buffer offset,
;   Music theme,
;   North scene,
;   South scene,
;   East scene,
;   West scene
; >

{{ $prefix := .Prefix -}}
{{ .Prefix }}_bg_buffer DB {{ .BufferSize }} DUP(0)
{{ .Prefix }}_fg_buffer DB {{ .BufferSize }} DUP(0)

{{ range $i, $s := .Scenes -}}
; --- Scene # {{ $i }} ---
{{ $s.Name }} SCENE <OFFSET {{ $prefix }}_bg_buffer + {{ $s.MapOffset }}, OFFSET {{ $prefix }}_fg_buffer + {{ $s.MapOffset }}, {{ $s.NorthName }}, {{ $s.SouthName }}, {{ $s.EastName }}, {{ $s.WestName }}>

{{ end }}
