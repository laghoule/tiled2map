; GENERATED BY tiled2map (https://github.com/laghoule/tiled2map)
; DO NOT EDIT

; <SCENE_NAME> SCENE <
;   BG buffer offset,
;   FG buffer offset,
;   Music theme,
;   North scene,
;   South scene,
;   East scene,
;   West scene
; >

map_bg_buffer DB {{ .BufferSize }} DUP(0)
map_fg_buffer DB {{ .BufferSize }} DUP(0)

{{ range $i, $s := .Scenes -}}
; --- Scene # {{ $i }} ---
{{ $s.Name }} SCENE < \
  OFFSET map_bg_buffer + {{ $s.MapOffset }}, \
  OFFSET map_fg_buffer + {{ $s.MapOffset }}, \
  OFFSET {{ $s.MusicName }}, \
  {{ $s.NorthName }}, \
  {{ $s.SouthName }}, \
  {{ $s.EastName }}, \
  {{ $s.WestName }} \
>

{{ end }}
