; GENERATED BY tiled2map (https://github.com/laghoule/tiled2map)
; DO NOT EDIT

; <SCENE_NAME> SCENE <
;   BG buffer offset,
;   FG buffer offset,
;   Music theme,
;   North scene,
;   South scene,
;   East scene,
;   West scene
; >

{{ range $i, $s := . }}
; --- Scene # {{ $i }} ---
{{ $s.Name }} SCENE < \
  <OFFSET world_bg_buffer + {{ $s.MapOffset }}, 0>, \
  <OFFSET world_fg_buffer + {{ $s.MapOffset }}, 1>, \
  OFFSET {{ $s.MusicName }}, \
  {{ $s.NorthName }}, \
  {{ $s.SouthName }}, \
  {{ $s.EastName }}, \
  {{ $s.WestName }}  \
>
{{ end }}
